@*  RISE PROJECT - 2026 - COPYRIGHT by Acanfora Giuseppe *@
@model List<RISE.Models.FAQ>

@{
    ViewData["Title"] = "Rules & FAQ";
}

<div class="container mt-4">

    <h2 class="mb-3">Competition Rules</h2>

    <p>You can download the official competition rules here:</p>

    <a href="/Files/Rules" class="btn btn-primary mb-5">
        Download Rules (PDF)
    </a>

    <hr class="my-5" />

    <h2 class="mb-3">Frequently Asked Questions</h2>

    <div class="mb-3">
        <button id="expandAllBtn" class="btn btn-sm btn-primary me-2">
            Expand All
        </button>
        <button id="collapseAllBtn" class="btn btn-sm btn-secondary">
            Collapse All
        </button>
    </div>

    <div class="faq-list">
        @for(int i = 0; i < Model.Count; i++)
        {
            <div class="faq-item border rounded mb-2">
                <div class="faq-question d-flex justify-content-between align-items-center p-2"
                     style="cursor:pointer;"
                     data-index="@i">
                    <span>@Model[i].Question</span>
                    <i class="fas fa-chevron-down" id="icon-@i"></i>
                </div>

                <div class="faq-answer p-3"
                     id="answer-@i"
                     style="display:none;">
                    @Model[i].Answer
                </div>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        document.querySelectorAll('.faq-question').forEach(el => {
            el.addEventListener('click', () => {
                const i = el.getAttribute('data-index');
                const answer = document.getElementById('answer-' + i);
                const icon = document.getElementById('icon-' + i);

                if (answer.style.display === 'none') {
                    answer.style.display = 'block';
                    icon.classList.remove('fa-chevron-down');
                    icon.classList.add('fa-chevron-up');
                } else {
                    answer.style.display = 'none';
                    icon.classList.remove('fa-chevron-up');
                    icon.classList.add('fa-chevron-down');
                }
            });
        });

        document.getElementById('expandAllBtn').addEventListener('click', () => {
            document.querySelectorAll('.faq-answer').forEach(a => a.style.display = 'block');
            document.querySelectorAll('.faq-question i').forEach(i => {
                i.classList.remove('fa-chevron-down');
                i.classList.add('fa-chevron-up');
            });
        });

        document.getElementById('collapseAllBtn').addEventListener('click', () => {
            document.querySelectorAll('.faq-answer').forEach(a => a.style.display = 'none');
            document.querySelectorAll('.faq-question i').forEach(i => {
                i.classList.remove('fa-chevron-up');
                i.classList.add('fa-chevron-down');
            });
        });
    </script>
}